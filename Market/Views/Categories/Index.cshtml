
@{
    ViewBag.Title = "Index";
}

<h2>Categories</h2>
@using (Html.BeginForm())
{
<button id="btnGetCategories">Get Categories</button>
<button id="btnGetCategory">Get Category</button>
<button id="btnAddCategory">Add Category</button>
<button id="btnUpdateCategory">Update Category</button>
<button id="btnDeleteCategory">Delete Category</button>
<br />
<br />
<input type="text"  id="txtCategoryID" placeholder="CategoryID" />
<input type="text"  id="txtDescription" placeholder="Category Description" />
<br />
<div id="lblMessage">   </div>
}

@section Scripts{
    <script type="text/javascript">
        $(function () {
            $('#btnGetCategories').on("click", function () {
                $.ajax({
                    type: 'GET',
                    
                })
                .success(function (data) {

                })
                .error(function (hxr, ajaxOptions, throwError) {

                });
                return false;
            });

            $('#btnGetCategory').on("click", function () {
                return false;
            });

            $('#btnAddCategory').on("click", function () {
                return false;
            });

            $('#btnUpdateCategory').on("click", function () {
                return false;
            });

            $('#btnDeleteCategory').on("click", function () {
                return false;
            });

            $(document).ajaxStart(function () {
                $("#loading").show();
            });

            $(document).ajaxStop(function () {
                $("#loading").hide();
            });
        });
    </script>    
}
    <style type="text/css">
        #loading{
            display: none;
            background-color: transparent;
            z-index: 999999;
            position: absolute;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            text-align: center;
            padding-top: 300px;
            filter: alpha(opacity = 75);
            -khtml-opacity: 0.75;
            -moz-opacity: 0.75;
            opacity: 0.75;
        }
    </style>
    <div id="loading"><img src="~/Images/loader.gif"/></div>

